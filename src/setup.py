#!/usr/bin/env python

from distutils.core import setup

setup(
    name="pend_acrobatics",
    version="0.1",
    description="Code for the integrated project",
    author="Florian Wolf",
    packages=["utils"],
)
